<%= form_with(model: step, local: true) do |form| %>
  <% if step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day %>
    <%= form.number_field :day, id: :step_day %>
  </div>

  <div class="field">
    <%= form.label :discription %>
    <%= form.text_area :discription, id: :step_discription %>
  </div>

  <div class="field">
    <%= form.label :target_id %>
    <%= form.text_field :target_id, id: :step_target_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
