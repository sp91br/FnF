<p id="notice"><%= notice %></p>
<!-- Links to Log in page -->
<h1>Users Name</h1>

<table>
  <thead>
    <tr>
      <!-- <th>Title</th>
      <th>Subtitle</th>
      <th>Info</th>
      <th>Url</th>
      <th>Description</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <p>
      <% @users.each do |user| %>
      <td><%= image_tag user.image %></td>
      <% end %>
      <a class="current_goal" data-toggle="collapse" href="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
      <button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
        <% @users.each do |user| %>
        <%= user.title.last %>
        <% end %>
      </button>
      </a>
    </p>
    <div class="current_goal_pic" id="collapse_picture">
      <div class="card card-block">
        <% @users.each do |user| %>
        <tr>
          <!--
        <td><%= user.subtitle %></td>
        <td><%= user.info %></td>-->
        <td><%= image_tag user.url %></td>
        <td><%= image_tag user.image %></td>
        <td><a><%= user.description %></a></td>
        <!-- <td><%= link_to 'Show MileStone Details', user %></td> -->
        <!-- links to show view -->
        <!-- <td><%= link_to 'Edit MileStone', edit_user_path(user) %></td> -->
        <!-- links to edit -->
        <!-- <td><%= link_to 'Delete MileStone', user, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        <!-- links to desrtoy -->
      </tr>
    <% end %>
    </div>
  </div>

    <!-- <div class="container">
  <h2>Simple Collapsible</h2>
  <button type="button" class="btn btn-info" data-toggle="collapse" data-target="#demo">Simple collapsible</button>
  <div id="demo" class="collapse">
    Lorem ipsum dolor sit amet, consectetur adipisicing elit,
    sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam,
    quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
  </div>
</div> -->
  </tbody>
</table>

<br>

<%= link_to 'Create new MileStone', new_user_path %>
<!-- links to create new user -->
